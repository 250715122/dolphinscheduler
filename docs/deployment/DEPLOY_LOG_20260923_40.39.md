# 部署过程记录：feat/local-dev → 测试机 192.168.40.39

| 项 | 内容 |
|----|------|
| 日期 | 2026-09-23 20:58–21:00（UTC+8） |
| 操作人 | Cursor Agent（via `ssh agi@192.168.40.39` / 构建机 `gt@192.168.56.101`） |
| 代码 | fork `250715122/dolphinscheduler` 分支 `feat/local-dev` @ `e4f4f33d5` |
| 目标机 | `AGI-DOLPHIN` / `192.168.40.39` |
| 安装目录 | `/opt/apache-dolphinscheduler-3.4.1-bin`（Standalone） |
| 元库 | PostgreSQL `192.168.41.85:5432/dolphinscheduler`（用户 `dolphin`） |
| 结果 | **成功**（health 全 UP；SSO URL 正常；UI 双登录入口已上） |

机器上同内容副本：`/home/agi/ds-backup/DEPLOY_RECORD_20260923.md`  
实时命令日志：`/home/agi/ds-backup/DEPLOY_20260923-205940.log`

---

## 1. 部署前现网状态

- 进程：StandaloneServer（PID 756，自 2026-07-29 起）
- JDK：`/home/agi/java/jdk-11.0.25`
- 端口：`12345`（API/UI）、`5678`（master）等
- 认证：`PASSWORD`（未接 Casdoor）
- 环境变量（`bin/env/dolphinscheduler_env.sh`）：
  - `DATABASE=postgresql`
  - `SPRING_PROFILES_ACTIVE=postgresql`
  - `SPRING_DATASOURCE_URL=jdbc:postgresql://192.168.41.85:5432/dolphinscheduler`
- UI/jar 布局：`*/libs/*.jar` → 符号链接到 `$DS/libs/`；UI 存在于 `ui/`、`api-server/ui/`、`standalone-server/ui/`
- `t_ds_version` = `3.4.1`
- `t_ds_project_preference.preferences` 原为 `varchar(512)`（部署中改为 `TEXT`）

---

## 2. 备份（回滚基线）

备份目录：

```text
/home/agi/ds-backup/20260923-205844
```

（路径也写在 `/home/agi/ds-backup/LAST_BACKUP.txt`）

| 内容 | 路径 | 大小约 |
|------|------|--------|
| 精简整包 tar（排除大量 gc 日志） | `ds-home-slim.tgz` | 369M |
| 旧 jar | `libs/dolphinscheduler-{api,service,dao}-3.4.1.jar` | ~1.4M |
| conf | `conf/standalone-server-conf/` + 后续 `application.yaml.pre-casdoor-patch` | — |
| env | `env/dolphinscheduler_env.sh` | — |
| UI tar | `ui/ui-top.tgz` / `ui-api.tgz` / `ui-standalone.tgz` | — |
| 元库 dump（custom 格式） | `meta/dolphinscheduler-pre.dump` | 3.0M |
| 进程/端口快照 | `meta/procs.txt` / `ports.txt` | — |

dump 在构建机执行：

```bash
PGPASSWORD=dolphin pg_dump -h 192.168.41.85 -U dolphin -d dolphinscheduler -Fc \
  -f /tmp/ds-release-40.39/dolphinscheduler-pre.dump
```

然后 scp 到测试机备份目录。

---

## 3. 构建与传输

在 `192.168.56.101`：

- 使用已编译产物：
  - `dolphinscheduler-api-3.4.1.jar`
  - `dolphinscheduler-service-3.4.1.jar`
  - `dolphinscheduler-dao-3.4.1.jar`
  - `dolphinscheduler-ui/dist`（含双登录入口）
- 从运行中的开发 conf 抽出 Casdoor 段，**redirect 改为**  
  `http://192.168.40.39:12345/dolphinscheduler/ui/login`  
  → `/tmp/ds-release-40.39/conf/casdoor-block.yaml`
- 整目录 scp 到测试机 `/tmp/ds-release-40.39/`

Casdoor 侧：为应用 `application_dolphinscheduler` 追加 redirect URI（同上）。

---

## 4. 停服与替换

1. `kill` StandaloneServer（PID 756），确认无残留。
2. 覆盖真实 jar（非 symlink）：
   - `/opt/apache-dolphinscheduler-3.4.1-bin/libs/dolphinscheduler-api-3.4.1.jar`
   - `.../dolphinscheduler-service-3.4.1.jar`
   - `.../dolphinscheduler-dao-3.4.1.jar`
3. `rsync --delete` UI → `ui/`、`api-server/ui/`、`standalone-server/ui/`
4. 修改 `standalone-server/conf/application.yaml`：
   - `security.authentication.type: CASDOOR_SSO`
   - 写入 Casdoor endpoint / client / cert / org / app / redirect（指向 40.39）
5. 元库（自 56.101）：

```sql
ALTER TABLE t_ds_project_preference ALTER COLUMN preferences TYPE TEXT;
```

6. 启动：`cd standalone-server && bash bin/start.sh`（`JAVA_HOME` 仍用 env 中的 JDK11）

约 36s UI 就绪。

---

## 5. 验证结果

| 检查项 | 结果 |
|--------|------|
| `GET /dolphinscheduler/ui/login` | 200 |
| `GET /actuator/health` | **UP**：alert / api / db / master / worker |
| `GET /login/sso` | 返回 Casdoor authorize URL，`redirect_uri` 含 `192.168.40.39:12345` |
| 原生登录 `admin`/`dolphinscheduler123` | `10013` 用户名或密码错误（测试机 admin 密码不是开发默认；错误码为密码类而非 SSO state，说明双登录密码路径已生效） |
| UI 资源 | `btn-login-password-option` 存在于 `ui/assets` |

建议人工再验：打开 http://192.168.40.39:12345/dolphinscheduler/ui/login  
应看到「单点登录」「账号密码登录」两选项；用正确本地账号或钉钉 SSO 登录；触发一次失败工作流看告警文案。

---

## 6. 回滚步骤（出问题使用）

```bash
export BK=$(cat /home/agi/ds-backup/LAST_BACKUP.txt)
# 当前：/home/agi/ds-backup/20260923-205844

# 6.1 停服
pkill -f 'org.apache.dolphinscheduler.StandaloneServer' || true
sleep 3
pkill -9 -f 'org.apache.dolphinscheduler.StandaloneServer' || true

# 6.2 恢复 jar
DS=/opt/apache-dolphinscheduler-3.4.1-bin
cp -a "$BK/libs/"*.jar "$DS/libs/"

# 6.3 恢复 conf（含认证类型）
cp -a "$BK/conf/standalone-server-conf/." "$DS/standalone-server/conf/"
# 若只需撤回 Casdoor：也可用
# cp -a "$BK/conf/application.yaml.pre-casdoor-patch" "$DS/standalone-server/conf/application.yaml"

# 6.4 恢复 UI
tar -C "$DS" -xzf "$BK/ui/ui-top.tgz"
tar -C "$DS/api-server" -xzf "$BK/ui/ui-api.tgz"
tar -C "$DS/standalone-server" -xzf "$BK/ui/ui-standalone.tgz"

# 6.5 如需整目录级恢复（更重）
# cd /opt && mv apache-dolphinscheduler-3.4.1-bin apache-dolphinscheduler-3.4.1-bin.bad
# tar -xzf "$BK/ds-home-slim.tgz"

# 6.6 仅当 schema/数据被破坏时恢复库（会丢部署后新数据）
# pg_restore -c -h 192.168.41.85 -U dolphin -d dolphinscheduler "$BK/meta/dolphinscheduler-pre.dump"

# 6.7 启服
cd "$DS/standalone-server" && bash bin/start.sh
curl -s http://127.0.0.1:12345/dolphinscheduler/actuator/health
```

**说明**：`preferences` 改为 `TEXT` 对旧版本一般兼容，通常不必回滚该 DDL；若必须回切 `varchar(512)`，需确认无超长 JSON 后再改。

---

## 7. 变更摘要

- Casdoor SSO + 原生账号双登录（登录页二选一）
- 工作流/任务超时告警中文可读标题与正文
- 项目偏好业务分组、跨项目血缘 UI 等（随 jar/ui 一并带上）
- 项目偏好字段扩为 TEXT，避免长 JSON 写库失败

---

## 8. 未决 / 后续

- [ ] 用测试机真实本地管理员密码验证账号密码登录
- [ ] 钉钉 SSO 走通一次完整回调
- [ ] 造一条失败工作流，确认钉钉告警为 `【调度失败】项目 / 实例名` 格式
- [ ] 如需长期保留本记录到 Git：提交本文件或仓库内 `docs/deployment/DEPLOY_LOG_20260923_40.39.md`
